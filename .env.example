# LLM Configuration
# Choose provider: 'nvidia' or 'ollama'
LLM_PROVIDER=nvidia

# API Keys (required for some providers)
NVIDIA_API_KEY=your-nvidia-api-key-here

# Model Selection (optional, will use provider defaults if not specified)
# For NVIDIA AI: deepseek-ai/deepseek-r1
# For Ollama: deepseek-coder:latest
LLM_MODEL=

# Output Directory Configuration
OUTPUT_DIR=out

# LaTeX Configuration
# Path to master resume template
MASTER_RESUME_PATH=resume/masterResume.tex
